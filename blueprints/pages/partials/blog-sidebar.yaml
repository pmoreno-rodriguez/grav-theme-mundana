form:
  fields:

    header.show_sidebar:
      type: toggle
      toggleable: true
      label: MUNDANA.ADMIN.BLOG.CONFIGURATION.SHOW_SIDEBAR
      highlight: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool
    
    header.show_featured:
      type: toggle
      toggleable: true
      label: MUNDANA.ADMIN.BLOG.CONFIGURATION.SHOW_FEATURED_POSTS
      highlight: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool
    header.show_categories:
      type: toggle
      toggleable: true
      label: MUNDANA.ADMIN.BLOG.CONFIGURATION.SHOW_CATEGORIES
      highlight: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool